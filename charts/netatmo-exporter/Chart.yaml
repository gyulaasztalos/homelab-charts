apiVersion: v2
name: netatmo-exporter
description: Netatmo weather-station Prometheus exporter (xperimental/netatmo-exporter) for the homelab.
icon: https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/svg/netatmo.svg
type: application
version: 0.1.0
# No appVersion on purpose — the deployed version is image.tag in values.yaml
# (single source of truth, Renovate-managed). See ddns-updater/Chart.yaml.
dependencies:
  - name: common
    version: ">= 0.3.0"
    repository: "file://../common"
